{
  "name": "@ghostclick/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ghostclick's UI. A Vite + Vue app that talks to the ghostclick backend over HTTP and one WebSocket.",
  "scripts": {
    "dev": "vite",
    "prebuild": "node scripts/check-lang.js --local",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test \"test/**/*.test.js\"",
    "check:lang": "node scripts/check-lang.js",
    "sync:lang": "node scripts/sync-lang.js"
  },
  "dependencies": {
    "@fontsource/instrument-serif": "^5.3.0",
    "@fontsource/inter": "^5.3.0",
    "pinia": "^4.0.3",
    "vue": "^3.5.42",
    "vue-router": "^5.3.1"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "@vitejs/plugin-vue": "^6.0.8",
    "tailwindcss": "^4.3.3",
    "vite": "^8.2.2"
  }
}
